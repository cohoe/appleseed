---
# Home with encrypted. Run the mac playbook first.
#
- name: Home
  hosts: all
  vars_files:
    - ../vars/global.yml
    - ../vars/home.yml
    - ../vars/claude-home.yaml
  roles:
    - claude
    - backups/nas
    - ssh
    - brave
    - steam
    - signal
